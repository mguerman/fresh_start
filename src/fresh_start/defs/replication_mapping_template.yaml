groups:

  # Test entities   - used for testing only
  - name: test_data
    enabled: true          # <-- enable/disable whole group
    schedule: daily
    dependency: null
    tables:
      - source_schema: cs_fdw
        target_schema: cs_raw
        table: ps_stdnt_fa_term 
        enabled: true
        transformation:
          enabled: false
          steps: ""  
          